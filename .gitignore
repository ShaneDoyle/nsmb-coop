*.nds
*.sav
*.sav.*

/__tmp
/backup
/build

# Written by `nsmbtool reference sync` from nsmbref.lock, and holding an
# absolute path into this machine's store. The lock is what gets committed.
/.ncpatcher.env

/.vscode
/.venv

*.lnk
*.xdelta
