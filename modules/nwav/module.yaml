id: NWAV
name: Nitro WAV
description: Adds stream-able audio to the game.

authors: TheGameratorT

targets:
  - arm9:
      includes: "include"
      sources: "source9/*"

components:
  - HasEvents:
      defines: NWAV_HAS_EVENTS

  - HasStereo:
      defines: NWAV_HAS_STEREO
